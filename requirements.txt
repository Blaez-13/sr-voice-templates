# sr-voice-templates Python dependencies
# Used by deploy/stamp.py and deploy/smoke.py

# PyYAML — for parsing workflow.yaml and scenario YAML files.
# Falls back to a minimal built-in parser if not installed,
# but full nested YAML (pass_criteria, turns, tool_mock) requires PyYAML.
PyYAML>=6.0

# requests and httpx are NOT required — the scripts use urllib from stdlib.
# Add one of these if the ElevenLabs simulation API requires multipart uploads:
# requests>=2.31.0
# httpx>=0.27.0
